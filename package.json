{
    "name": "ccext-filesystem",
    "version": "0.1.0",
    "author": "Alex Cui",
    "license": "Apache-2.0",
    "scripts": {
        "build": "rimraf ./build && mkdirp build && rimraf ./dist && mkdirp dist && webpack --bail",
        "build:dist": "NODE_ENV=production yarn run build"
    },
    "dependencies": {
        "clipcc-extension": "^0.1.7"
    },
    "devDependencies": {
        "@types/node": "^17.0.16",
        "copy-webpack-plugin": "^10.2.4",
        "mkdirp": "^1.0.4",
        "rimraf": "^3.0.2",
        "ts-loader": "^9.2.6",
        "typescript": "^4.5.5",
        "webpack": "^5.68.0",
        "webpack-cli": "^4.9.2",
        "zip-webpack-plugin": "^4.0.1"
    }
}
